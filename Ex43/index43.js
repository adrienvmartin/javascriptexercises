let text = "game of thrones";
let firstChar = text.charAt(0);
let secondChar = text.charAt(5);
let thirdChar = text.charAt(8);

let result = firstChar + secondChar + thirdChar;
let message = `The final result is ${result.toUpperCase()}`;

console.log(message);