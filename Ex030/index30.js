let sideLength = 5;
let area = Math.pow(sideLength, 2);

let message = `The area of a square can be calculated by multiplying the length and width together, or squaring the side length (since they're all the same on a square). The length of our side is ${sideLength}, therefore the area is ${sideLength} squared, or ${area}`;

console.log(message);