let firstSide = 10;
let secondSide = 20;
let thirdSide = 5;

let perimeter = firstSide + secondSide + thirdSide;

console.log(`The perimeter of a triangle can be calculated by adding all three sides. In our case, the sides are 10, 20, and 5 in length, and thus the perimeter is ${perimeter}`);