let sideLength = 10;

let message = `The perimeter of a square can be calculated by the formula 4S, where "S" equals the side length. The side length of our square is ${sideLength}. Four times our side length is equal to ${sideLength * 4}`;

console.log(message);